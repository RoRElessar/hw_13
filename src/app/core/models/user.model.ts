export class User {
  id: number;
  // tslint:disable-next-line:variable-name
  first_name: string;
  // tslint:disable-next-line:variable-name
  last_name: string;
  email: string;
  region: string;
  image: string;
  sex: string;
  subscription: boolean;
  // tslint:disable-next-line:variable-name
  additional_information: string;
}
